{% extends 'base.html.twig' %}

{% block body %}
    <h1>Categorie_sub_sub</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie_sub_sub.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ categorie_sub_sub.title }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('categorie_sub_sub_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('categorie_sub_sub_edit', { 'id': categorie_sub_sub.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
