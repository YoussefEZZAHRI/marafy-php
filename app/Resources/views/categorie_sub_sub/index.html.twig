{% extends 'base.html.twig' %}

{% block body %}
    <h1>Categorie_sub_sub list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for categorie_sub_sub in categorie_sub_subs %}
            <tr>
                <td><a href="{{ path('categorie_sub_sub_show', { 'id': categorie_sub_sub.id }) }}">{{ categorie_sub_sub.id }}</a></td>
                <td>{{ categorie_sub_sub.title }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('categorie_sub_sub_show', { 'id': categorie_sub_sub.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('categorie_sub_sub_edit', { 'id': categorie_sub_sub.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('categorie_sub_sub_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
